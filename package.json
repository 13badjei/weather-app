{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "creating a weather nodejs app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "requests": "^0.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
